---
title: Overview
order: 1
---

seek is built with Bun and TypeScript and compiles to a single-file native
binary. Each command groups providers by intent — search, research, or scrape —
so you can choose speed, cost, and output format per call.

It is designed to be driven by an agent as easily as by a human. Rather than
exposing one provider, it groups providers under intent-shaped commands so the
caller expresses what they want, not which vendor to hit.
